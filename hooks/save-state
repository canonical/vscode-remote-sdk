# save-state must never block a refresh, even in a broken container.
trap 'exit 0' ERR

[ -n "$SDK_STATE_DIR" ] || exit 0

for key_file in /etc/ssh/ssh_host_*_key /etc/ssh/ssh_host_*_key.pub; do
    [ -e "$key_file" ] || continue
    cp --archive -- "$key_file" "$SDK_STATE_DIR/"
done
